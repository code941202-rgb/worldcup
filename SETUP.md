# 🛠️ 멀티유저 설정 가이드 (Supabase)

이 앱은 Supabase(무료 DB)에 모든 참가자의 예측을 저장해서, 관리자가 전원 결과를 모아 보고 마감/채점할 수 있습니다.

## 1단계. Supabase 프로젝트 준비
1. https://supabase.com 로그인 → 기존 프로젝트 선택 (또는 New project)
2. 프로젝트가 만들어지면 좌측 메뉴 **SQL Editor** 열기

## 2단계. 테이블/함수 생성
1. 이 폴더의 `supabase/schema.sql` 내용을 전부 복사
2. SQL Editor에 붙여넣고 **Run** 클릭
3. 에러 없이 `Success`가 뜨면 완료 (events, predictions 테이블 + 함수 생성됨)

## 3단계. 연결 키 입력
1. Supabase 좌측 메뉴 **Project Settings → API** (또는 Data API)
2. 아래 두 값을 복사:
   - **Project URL** (예: `https://abcd1234.supabase.co`)
   - **anon public** key (긴 문자열, 공개되어도 안전한 키)
3. 이 폴더의 `config.js` 를 열어 붙여넣기:
   ```js
   const SUPABASE_URL = "https://abcd1234.supabase.co";
   const SUPABASE_ANON_KEY = "eyJhbGciOi...(여기에 anon key)";
   ```

## 4단계. 배포
```
git add .
git commit -m "supabase 연동"
git push
```
Vercel이 자동 재배포합니다.

---

## 사용 방법

### 관리자(나)
1. 홈에서 **새 이벤트 만들기** → 이름/분류(학교·직원·기타)/관리자 PIN 입력 → 생성
2. 생성되면 참여 코드와 링크가 나옴 → 참가자들에게 공유
3. 참가자들이 제출하면 **관리자 탭**에서 전원 예측 확인
4. **1차(32강) 마감** 버튼 → 이후 참가자는 32강을 못 바꾸고 16강부터만 수정 가능
5. **2차(전체) 마감** 버튼 → 모든 수정 잠금
6. **실제 결과 입력**: 실제 경기 결과대로 대진을 채우고 저장 → 자동 채점

### 참가자
1. 받은 링크로 입장 (또는 홈에서 참여 코드 입력)
2. **내 예측** 탭에서 이름 입력 → 32강 배치 후 우승까지 예측 → **제출/저장**
3. 같은 이름으로 다시 들어오면 이어서 수정 가능 (마감 전까지)

### 순위·통계 탭
- 1차(32강 적중률) / 2차(전체 적중률) 정렬 전환
- "우승국 맞힌 사람만" 필터, 이름 검색
- 참가자 클릭 → 그 사람의 32강~우승 전체 예측을 펼쳐서 확인
- 평균 적중률, 최고 적중률, 우승국 적중자 수 등 통계 카드

## 분류(이벤트) 구분
- 학교 후배용 / 직원 이벤트용을 **별도 이벤트**로 만들면 완전히 분리됩니다.
- 홈 화면의 학교/직원/기타 필터로 골라 입장하세요.

---

## 🥈 2차 이벤트 (16강~우승 맞추기) 운영 방법

2차는 "실제 32강 진출국"을 기준으로 16강부터 우승까지만 맞추는 방식입니다.

> 먼저 `supabase/update-phase2.sql` 을 SQL Editor에서 한 번 실행해 두세요. (2차 규칙 반영)

권장: **2차는 새 이벤트로 생성**하면 1차 기록과 섞이지 않아 깔끔합니다.

1. 🛡️ 관리자 → 새 이벤트 만들기 (예: "직원 월드컵 2차")
2. 관리자 탭 → **🎯 실제 결과 입력** → 실제 32강 진출 32개국을 각 자리에 배치하고 저장
3. 관리자 탭 → **🔒 1차(32강) 마감** 클릭
   - 이제 참가자 화면의 32강은 "실제 진출국"으로 고정되고, 16강부터 우승까지만 선택 가능
4. 참여 코드/링크를 참가자에게 공유 → 참가자는 16강~우승 선택 후 제출
5. 경기가 진행되면 관리자가 **🎯 실제 결과 입력**에서 16강·8강·4강·결승·우승 승자를 채워 저장
6. 순위·통계 탭 → **"2차: 16강~우승 적중률"** 선택
   - 32강은 점수에서 제외, 16강·8강·4강·결승 진출 + 우승(가중치 3배)으로 채점
   - "우승국 맞힌 사람만" 필터로 우승 적중자끼리 비교 가능
   - 참가자 클릭 시 32강~우승 전체 예측을 펼쳐 확인
